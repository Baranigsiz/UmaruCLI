version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: {{.SafeName}}-api
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - APP_ENV=production
    restart: unless-stopped
