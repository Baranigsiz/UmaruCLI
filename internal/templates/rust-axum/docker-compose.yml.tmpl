version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: {{.SafeName}}-app
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - RUST_LOG=info
    restart: unless-stopped
