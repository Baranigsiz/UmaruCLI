package handlers

import (
	"net/http"

	"github.com/labstack/echo/v4"
)

func Welcome(c echo.Context) error {
	return c.JSON(http.StatusOK, map[string]string{
		"message": "Welcome to {{.ProjectName}} API built with Echo! 🌐🚀",
		"docs":    "/health",
	})
}
