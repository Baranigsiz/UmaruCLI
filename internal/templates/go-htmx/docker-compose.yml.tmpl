version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: {{.SafeName}}-web
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - APP_ENV=production
    restart: unless-stopped
